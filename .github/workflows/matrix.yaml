name: Matrix

on: push

jobs:
  node-version:
    runs-on: ubuntu-latest
    steps:
      - name: Log node Version
        run: node node -v 
      - uses: actions/setup-node@v2
        with:
          node-version: '14'
      - name: Log node Version
        run: node node -v
      